
var Sprite = require('../../../../src/Sprite');
var expect = require('chai').expect;

describe('Constructor', function(){

  it ('must create an Sprite with defaults');
  it ('must throw an error if no texture');

});